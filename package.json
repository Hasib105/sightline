{
  "name": "sightline",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "description": "Sightline monorepo for the Django API, Next.js web app, inference worker, docs, and infrastructure.",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "api:install": "uv sync",
    "api:makemigrations": "uv run python apps/api-django/manage.py makemigrations sightline",
    "api:migrate": "uv run python apps/api-django/manage.py migrate",
    "api:seed": "uv run python apps/api-django/manage.py seed_sightline",
    "api:seed-if-empty": "uv run python apps/api-django/manage.py seed_sightline --if-empty",
    "api:dev": "uv run uvicorn sightline_api.asgi:application --app-dir apps/api-django --host 127.0.0.1 --port 8000",
    "api:runserver": "uv run python apps/api-django/manage.py runserver 127.0.0.1:8000",
    "backend:install": "pnpm run api:install",
    "web:install": "pnpm install --filter sightline-web",
    "web:dev": "pnpm --filter sightline-web dev",
    "web:build": "pnpm --filter sightline-web build",
    "web:lint": "pnpm --filter sightline-web lint",
    "web:preview": "pnpm --filter sightline-web preview",
    "dev": "concurrently \"pnpm run web:dev\" \"pnpm run api:dev\" --names web,api -c blue,green",
    "install:all": "pnpm install && pnpm run api:install",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "docker:seed": "docker compose exec api python manage.py seed_sightline",
    "docker:prod:config": "docker compose -f compose.prod.yml config",
    "docker:prod:up": "docker compose -f compose.prod.yml up -d",
    "docker:prod:down": "docker compose -f compose.prod.yml down"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "cross-env": "^10.1.0"
  }
}
